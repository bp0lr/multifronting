# Sample input

`example.test.txt` illustrates the one-host-per-line format using reserved names.
These are placeholders, not working endpoints. Configure any test environment
separately under your control.

The old third-party domain lists have been removed from the working tree. They
are not used by tests or CI.
